# Timeline feature

- 목표: 원본에서 대중화까지의 핵심 변화를 시간 순서로 읽게 한다.
- 데이터: 정렬된 `TimelineEvent[]`.
- 경계: 이벤트의 시각적 순서와 유형 표현을 소유한다.
- 링크: 검토 근거가 있는 이벤트는 `sourceUrl`과 `sourceLabel`로 원문을 연결한다.
- 한계: 현재 날짜는 사람이 검토한 표시 문자열이며 자동 정렬하지 않는다.
